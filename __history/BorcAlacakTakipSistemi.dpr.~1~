program BorcAlacakTakipSistemi;

uses
  Forms,
  kullanicigiriss in 'kullanicigiriss.pas' {Form1},
  mainn in 'mainn.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
